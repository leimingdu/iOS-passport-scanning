# iOS_passport_scanning
passport scanning function for iOS platform, using the tesseract lib. Every passport can be scanned within 10s.

Install:

after download the source code, perform the command: pod install --verbose

